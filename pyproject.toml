[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.13.0, <3.14"
dependencies = [
    "django",
    "django-ninja",
    "psycopg[binary]",
    "granian[reload]",
    "httpx",
]

[tool.uv.sources]
backend = { path = "." }
